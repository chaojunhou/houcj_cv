\ProvidesPackage{info}
\RequirePackage{metalogo}

% moderncv themes
\moderncvtheme[grey]{classic} % optional argument are 'blue' (default), 'orange', 'red', 'green', 'grey' and 'roman' (for roman fonts, instead of sans serif fonts)
%\moderncvtheme[green]{casual}   % idem

% adjust the page margins
\usepackage[scale=0.8]{geometry}

% personal data
\firstname{候} % for chinese
\familyname{超俊}
\homepage{www.houcj.net}
\email{chaojunhou@126.com}
\mobile{(+86)159-8933-0052}




\AtBeginDocument{\renewcommand{\labelitemi}{-- }}

\usepackage[firstyear=2009,lastyear=2016]{moderntimeline}
%\tlmaxdates{1999}{2012}
\tlwidth{0.8ex}


\usepackage{fontspec}
\defaultfontfeatures{Ligatures=TeX}

\usepackage{microtype}



% Proper alignment
\usepackage[originalcommands]{ragged2e}
\renewcommand*{\cvcomputer}[4]{%
  \cvitem{#1}{\small\raggedright#2}{#3}{\small\raggedright#4}}

% Not all fonts have an sl shape
\renewcommand*{\cventry}[6]{%
  \cvline{#1}{%
    {\bfseries#2}%
    \ifx#3\else{, {\itshape#3}}\fi%
    \ifx#4\else{, #4}\fi%
    \ifx#5\else{, #5}\fi%
    .%
    \ifx#6\else{\newline{}\begin{minipage}[t]{\linewidth}\small#6\end{minipage}}\fi
    }}%


% Refine quote 
\newcommand*{\myquote}[2]{%
   \quote{\itshape #1 \\ \scshape \footnotesize #2}}

% Hobbies
\newcommand*{\cvhobby}[2]{\cvline{#1}{\small #2}}


